import React, { Component } from "react";
import Router from "./Route";

export default class App extends Component {
  render() {
    return (
      <div id="app">
        <Router />
      </div>
    );
  }
}
